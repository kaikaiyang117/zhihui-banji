# 移动端适配 TODO

> 目标：手机端（375–640px）访问时，界面不形变、可正常操作。

## 方案：iOS Tab Bar 模式

侧栏在手机端下移至底部，变为水平固定导航栏，符合 Apple HIG 移动端规范。

## 任务拆分

- [ ] **断点系统** — 新增 `≤640px`（手机）、`641–900px`（平板/小窗）、`901–1200px`（桌面）三档响应式
- [ ] **侧栏→底部 Tab Bar**（≤640px）
  - 固定底部，`flex-direction: row`，高度约 56px
  - 仅显示图标 + 短标签，隐藏 header/footer/分组标题
  - 安全区域适配（`padding-bottom: env(safe-area-inset-bottom)`）
- [ ] **顶栏精简**（≤640px）— 减小 padding，图标缩小至 14px
- [ ] **主内容区**（≤640px）
  - `padding: 14px`，`max-width: 100%`
  - 页面标题缩小至 20px
  - 卡片 `padding: 16px`，`border-radius: 12px`
- [ ] **概览卡片** — 手机端 2 列网格，`gap: 8px`
- [ ] **搜索框/输入框** — 手机端 `width: 100%`
- [ ] **表格** — 确保 `table-wrap` 横向滚动可用，添加右侧渐变遮罩提示可滚动
- [ ] **弹窗** — 手机端 `width: 95%`，`padding: 20px`，表单单列排布
- [ ] **侧栏收窄**（641–900px）— 仅显示图标（56px 宽），hover 时展开标签
- [ ] **图表** — `height` 自适应（手机 250px）
- [ ] **座位表** — 手机端单元格最小 32px
- [ ] **touch 增强** — 按钮最小点击区域 44px，行高加至 44px
