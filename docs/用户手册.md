# 美美大王工作台 v2.2 — 用户手册

> 汶川县七一映秀中学 · 班主任班级管理 + 个人健康追踪一体平台

---

## 1. 快速上手

### 1.1 启动工作台

**macOS**：双击 `启动工作台.command` 或在终端执行：
```bash
./启动工作台.command
```

**Windows**：双击 `启动工作台.bat`

启动后在浏览器打开 `http://localhost:5000`（Windows/macOS 默认 5000 端口）。如需更换端口，可设置 `WORKBENCH_PORT`。

如需让同一局域网中的手机或平板访问电脑上的工作台，可使用 `python backend/run.py --lan` 启动局域网模式。该模式仅适合可信局域网，不要将端口映射到公网。

### 1.2 界面布局

```
┌─────────────────────────────────────────────┐
│  🏫 教师工作台  │  👤 个人工作台             │  ← 顶部 Tab 切换
├────────┬────────────────────────────────────┤
│ 概览   │                                    │
│ 学生管理│        主内容区                    │
│ 教学管理│     (工作表 / 图表 / 数据表)       │
│ 班级事务│                                    │
│ 日志   │                                    │
├────────┴────────────────────────────────────┤
```

- **左侧**：导航菜单，按模块分组
- **右侧**：主内容区，显示当前模块的数据和操作

### 1.3 通用操作

| 操作 | 方式 |
|------|------|
| 添加记录 | 点击页面右上角带颜色的按钮（如 `📋 添加考勤`） |
| 删除记录 | 行末 `✕` 按钮，确认后删除 |
| 查看全部 | 数据表支持滚动浏览 |
| 搜索过滤 | 输入关键字后按 Enter 实时过滤（支持的表页） |
| 导出 Excel | 点击 `📥 导出Excel` 按钮，下载当前表数据 |
| 刷新数据 | 操作后数据自动刷新，无需手动 F5 |

---

## 2. 教师工作台

### 2.1 首页仪表盘

进入工作台默认打开的页面，提供班级整体概况：

- **班级人数** — 当前录入的学生总数
- **出勤/迟到** — 今日考勤统计（来自考勤管理表最新数据）
- **班费余额** — 滚动计算的当前余额
- **积分排行榜 TOP5** — 日常行为积分总分最高的五名学生
- **最近日志** — 最近五条班主任日志摘要
- **快捷操作** — 直达考勤、家校沟通、写日志、成绩查看等页面

### 2.2 学生管理

#### 2.2.1 学生信息

管理班级学生基础档案（学号、姓名、性别、出生年月、民族、家庭住址、监护人信息、是否住校、特长、班级任职、备注共13个字段）。

| 操作 | 说明 |
|------|------|
| `⬆️ 导入Excel` | 先检查预览，再确认导入/更新学生信息，按**学号**去重合并 |
| `📋 模板下载` | 下载标准导入模板（含列名与示例行） |
| `📥 导出Excel` | 导出当前全部学生数据 |
| 搜索框 | 按学号或姓名模糊搜索 |
| `✕` 删除 | 删除单个学生记录 |

**导入规则**：点击“检查文件”后，系统只解析 Excel，不会立即修改数据库；确认预览结果后才提交。学号为唯一标识，模板中已有学号则更新其他字段，新学号则新增。缺少学号、姓名或文件内重复学号的记录会被跳过并提示。

#### 2.2.2 特殊学生档案

记录需要特别关注的学生（如留守儿童、学习困难、行为偏差等），支持添加、删除和导出。

#### 2.2.3 评语管理

管理学生的学期评语、毕业评语、日常评语。支持草稿/已完成/已发送三种状态。

### 2.3 教学管理

#### 2.3.1 考勤管理

每日考勤记录，支持四种状态：**出勤 / 迟到 / 请假 / 缺勤**。

- 顶部统计卡片展示各状态累计数量
- 支持按关键字搜索考勤记录
- `📊 导出汇总报表`：按日期范围汇总各状态人数，输出汇总 Excel

#### 2.3.2 成绩跟踪

管理六科（语文、数学、英语、政治、历史、地理）的月考和期中考试成绩。

**页面内容：**
- **班级成绩概览** — 各科平均分（月考1 / 期中）
- **成绩分布图** — ECharts 柱状图对比每位学生的两次考试总分
- **学生成绩明细** — 完整成绩表，包含进退步列（↑/↓）

**派生计算（自动生成，无需手动填写）：**
- 月考1总分、期中总分
- 进退步 = 月考1排名 - 期中排名（正数=进步，负数=退步）

**导出：**
- `📥 导出明细` — 完整成绩表
- `📊 月考1汇总` / `📊 期中汇总` — 单次考试汇总报表

#### 2.3.3 行为积分

每周记录学生行为积分（8周），生成积分排行榜和走势图。

**页面内容：**
- **积分排行榜** — 前十名学生按总分降序
- **积分走势图** — TOP5 学生的每周积分折线图
- **积分明细表** — 全量数据展示

**派生计算**：月合计（W1-W8 总和）、班级排名（按总分降序）

### 2.4 班级事务

#### 2.4.1 座位表

可视化展示班级座位布局。座位表中包含学生姓名和特殊标记（讲台、前门、后门、过道）。座位编辑需通过对话让系统辅助修改。

#### 2.4.2 家校沟通记录

记录每次家校沟通的详细信息：日期、学生、沟通方式（电话/微信/面谈/家访/短信）、原因、内容摘要、家长反馈、后续跟进、完成状态。

#### 2.4.3 谈心记录

记录与学生的谈心过程：日期、学生姓名、谈心原因、地点、内容摘要、学生状态、后续计划、下次跟进时间。

#### 2.4.4 班会记录

班会组织记录：日期、周次、主题、形式（主题班会/事务通知/团队活动/安全教育/心理健康）、主要内容、学生参与情况、效果评估。

#### 2.4.5 班费管理

班费收支流水记录。

**派生计算**：余额 = 收入累计 - 支出累计（按记录顺序滚动计算）

每笔记录包含：日期、收支类型、金额、用途说明、经手人、证明人、备注。

#### 2.4.6 班委管理

记录班级干部信息：职务、姓名、职责描述、任职时间、备注。

#### 2.4.7 班级活动

记录班级活动：日期、活动名称、类型（文体活动/社会实践/志愿服务/学科竞赛/节日庆祝/其他）、参与人数、活动总结、获奖情况、佐证材料。

### 2.5 日志

#### 2.5.1 班主任日志

每日工作日志：日期、星期、天气、主要工作、突发事件、今日反思、待办事项。

#### 2.5.2 工作计划总结

学期工作计划与总结：学期、计划内容、总结内容。

---

## 3. 个人工作台

### 3.1 健康追踪

集成体重体脂、运动、睡眠三大健康管理维度。

#### 3.1.1 体重体脂追踪

**派生计算：**
- 腰臀比 = 腰围 / 臀围
- 与上周对比 = 本周体重 - 上周体重（自动生成，下拉即可查看趋势）

体重趋势图展示体脂变化曲线，默认标注 120 斤目标线。

#### 3.1.2 运动记录

记录每次运动：日期、运动类型（力量训练/有氧/拉伸/散步/其他）、具体项目、时长、强度、消耗热量、运动感受、是否完成。

#### 3.1.3 睡眠记录

记录每日睡眠：入睡时间、起床时间、睡眠时长、睡眠质量（优/良/一般/差）、起床感受、午休时长。

---

### 3.2 知识库

基于 Markdown 的笔记管理，与 Obsidian 深度集成。

**功能：**
- `📝 新建笔记` — 输入标题、选择分类和模板后创建空白笔记
- `🔗 打开 Obsidian` — 在本地 Obsidian 中打开知识库
- 笔记卡片显示名称、最后修改日期、文件大小
- 点击笔记卡片直接在 Obsidian 中打开编辑

**内置模板（创建笔记时可选）：**

| 模板 | 用途 |
|------|------|
| 备课笔记 | 课题、教学目标、重难点、教学过程、反思 |
| 班会记录 | 日期、主题、内容、学生反馈、后续跟进 |
| 班主任日志 | 日期、记事、好人好事、问题、明日计划 |
| 学生档案 | 基本信息、学业表现、行为记录、重点关注 |
| 考研知识点 | 科目、核心概念、记忆口诀、真题链接、复习记录 |
| 读书笔记 | 书籍信息、核心观点、精彩摘录、思考、行动清单 |

知识库文件存储在 `知识库/` 目录下，按分类创建子文件夹，可直接用 Obsidian 打开整个目录使用。

### 3.3 考研备考

（建设中）规划包含：学习计划制定、知识点追踪、真题练习、进度统计。

---

## 4. 数据管理

### 4.1 数据库

所有数据存储在 `data/workbench.db`（SQLite，WAL 模式），自动创建，无需手动管理。建议定期备份此文件。

### 4.2 派生计算列

以下列由系统在读取时自动计算，**无需手动填写，也不应覆盖**：

| 工作表 | 派生列 | 计算规则 |
|--------|--------|---------|
| 成绩跟踪 | 月考1总分 | 六科成绩之和 |
| 成绩跟踪 | 期中总分 | 六科成绩之和 |
| 成绩跟踪 | 进退步 | 月考1排名 - 期中排名 |
| 日常行为积分 | 月合计 | W1-W8 之和 |
| 日常行为积分 | 排名 | 按总分降序 |
| 班费管理 | 余额 | 收入累计 - 支出累计 |
| 体重体脂追踪 | 腰臀比 | 腰围 / 臀围 |
| 体重体脂追踪 | 对比上周 | 本周体重 - 上周体重 |

### 4.3 导入导出

- **学生信息导入**：支持 Excel 导入，按学号自动合并去重
- **通用导出**：所有工作表均支持一键导出为 Excel（`📥 导出Excel` 按钮）
- **汇总报表**：成绩和考勤支持导出专门的汇总报表

---

## 5. 常见问题

**Q: 启动后浏览器访问被拒绝？**
如果 5000 端口被其他程序占用，可使用 `python backend/run.py --port 8080` 启动，再访问 `http://localhost:8080`。

**Q: 学生信息导入失败？**
请确保 Excel 表头包含「学号」和「姓名」两列（不带多余空格/换行），缺少学号的行会被跳过。建议先下载标准模板填数据。

**Q: 总分/余额等列显示不对？**
派生列由系统自动计算，请勿在添加记录时手动填入这些列的值。如果已填入错误值，删除对应行后重新添加即可。

**Q: 知识库在哪里？**
知识库文件存储在项目根目录的 `知识库/` 文件夹中。可在工作台内创建笔记，也可直接用 Obsidian 打开该目录（`打开 Obsidian` 按钮）。

**Q: 如何备份数据？**
复制 `data/workbench.db` 文件即可完成全量备份。建议定期备份。

**Q: 数据表中如何搜索？**
部分页面（如考勤管理）的数据表有搜索框，输入关键字后按 Enter 即可过滤。学生信息页面的搜索框按学号或姓名搜索。
